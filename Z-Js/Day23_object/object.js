const user = {
    name:"rohit",
    age: 20,
    surname: "negi"
}

// access
console.log(user.name);
console.log(user["name"]);

// Create read update delete, CRUD
user.adhar = 1234;// new value inserted, update same.

